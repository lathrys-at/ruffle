name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  lint:
    name: fmt / clippy / doc
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Format
        run: cargo fmt --all --check
      - name: Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
      # The workspace's default-members scope plain cargo invocations to the core
      # crate, so the bindings crate is linted by name.
      - name: Clippy (Python extension crate)
        run: cargo clippy -p ruffle-py --all-targets -- -D warnings
      # Version lockstep holds on every PR, not only at release time: every binding
      # crate carries the core crate's version, so the workspace has exactly one
      # version across all members.
      - name: Version lockstep
        run: |
          versions="$(cargo metadata --no-deps --format-version 1 \
            | jq -r '.packages[] | "\(.name) \(.version)"')"
          count="$(echo "$versions" | awk '{print $2}' | sort -u | wc -l)"
          if [ "$count" -ne 1 ]; then
            echo "::error::workspace crate versions have drifted apart"
            echo "$versions"
            exit 1
          fi
      - name: Doc
        run: cargo doc --no-deps --all-features
        env:
          RUSTDOCFLAGS: "-D warnings"

  # Build + test in both profiles: debug keeps debug-assertions and integer-overflow
  # checks on; release runs the optimizer with them off, catching the other class of
  # bug. Always --all-features: the `cli` feature gates the binary and its process
  # tests, and a default-features run leaves that half of the crate untested.
  test:
    name: test (${{ matrix.profile }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - { profile: debug, flags: "" }
          - { profile: release, flags: "--release" }
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Build
        run: cargo build --verbose --all-features ${{ matrix.flags }}
      - name: Test
        run: cargo test --verbose --all-features ${{ matrix.flags }}

  msrv:
    name: msrv (1.85)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.85.0
      - uses: Swatinem/rust-cache@v2
      - name: Build on MSRV
        run: cargo build --verbose --all-features

  deny:
    name: cargo-deny (licenses + advisories)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check licenses bans sources

  # The Python bindings, driven by tox: the wheel is built and the full pytest suite
  # runs against it (parity replay, idiomatic tests, the mypy --strict gate, and the
  # Rust-CLI interop round trip) on every supported CPython version, floor first.
  # The matrix mirrors the tox envlist; the wheel itself is abi3, one build for the
  # floor and every version after.
  python:
    name: python (${{ matrix.python }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - name: Install tox
        run: python -m pip install tox
      - name: Test
        working-directory: bindings/python
        run: tox -e py$(echo "${{ matrix.python }}" | tr -d .)
      - name: Lint (ruff)
        if: matrix.python == '3.13'
        working-directory: bindings/python
        run: tox -e lint

  # The sdist is a maturin-vendored copy of a Cargo workspace member with a path
  # dependency on the workspace root, the layout where a vendored tarball most often
  # stops building. This job proves the README's from-source claim: build the sdist,
  # install it with pip in a clean environment (compiling through the Rust
  # toolchain), and replay the parity and API suites against the installed package.
  python-sdist:
    name: python (sdist install)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build the sdist
        run: |
          python -m pip install maturin
          maturin sdist --manifest-path bindings/python/Cargo.toml --out dist
      - name: Install from the sdist
        run: python -m pip install dist/*.tar.gz
      - name: Test the installed package
        run: |
          python -m pip install pytest
          python -m pytest bindings/python/tests/test_parity.py bindings/python/tests/test_api.py -q

  # Single aggregating check for the `main` branch protection rule: require only
  # "ci-ok". It runs always() and fails unless every other job succeeded, so a failed,
  # cancelled, or skipped job blocks the merge — while the protection rule stays one
  # stable check name (no need to re-list jobs or matrix legs as CI grows).
  ci-ok:
    name: ci-ok
    if: always()
    needs: [lint, test, msrv, deny, python, python-sdist]
    runs-on: ubuntu-latest
    steps:
      - name: Require all CI jobs to have succeeded
        if: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') || contains(needs.*.result, 'skipped') }}
        run: |
          echo "::error::a required CI job did not succeed: ${{ join(needs.*.result, ', ') }}"
          exit 1
